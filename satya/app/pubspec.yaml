name: satya
description: Satya — online AI fact-check app for India.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  image_picker: ^1.1.2
  receive_sharing_intent: ^1.8.0
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.1
  share_plus: ^10.1.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
